import Exec from "../components/exec";
import Navbar from "../components/navbar";

const ExecBoard = () => {
  return (
    <div>
      <Navbar />
      <Exec />
    </div>
  );
};

export default ExecBoard;
